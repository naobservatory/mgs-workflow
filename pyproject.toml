[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mgs-workflow"
version = "3.4.0.0-dev"
requires-python = ">=3.12"
dependencies = [
    "biopython>=1.85",
    "boto3",
    "frictionless>=5.18.0",
    "jsonschema>=4.20.0",
    "mypy>=1.18.1",
    "pandas-stubs",
    "types-PyYAML",
    "types-jsonschema",
    "packaging>=24.0",
    "pandas>=2.3.2",
    "pysam>=0.23.3",
    "pytest>=8.4.2",
    "ruff>=0.13.0",
]

[tool.setuptools]
packages = []

# Tool configurations below:
# - [tool.mypy]: Type checking configuration
# - [tool.ruff]: Linting and formatting configuration

[tool.mypy]
python_version = 3.12
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true

[[tool.mypy.overrides]]
module = ["pysam", "pysam.*", "Bio", "Bio.*", "tomli", "boto3", "boto3.*", "botocore", "botocore.*"]
ignore_missing_imports = true

[tool.ruff]
# Comprehensive rule selection
lint.select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes (unused imports, etc.)
    "I",   # isort
    "B",   # bugbear (likely bugs and design problems)
    "C4",  # comprehensions (better list/dict comprehensions)
    "UP",  # pyupgrade (modernize Python code)
    "N",   # naming (PEP8 naming conventions)
    "PIE", # misc. lints (unnecessary code)
    "RET", # return statements (consistent returns)
    "SIM", # simplify (code simplification)
]

# Ignored rules
lint.ignore = [
  "E402", # Ignore module level import not at top of file
  "E501", # Ignore line too long
  "N802", # Ignore function name should be lowercase
]

# Basic exclusions
exclude = [
    ".git",
    "__pycache__",
    "build",
    "dist",
    ".venv",
    "tmp",
    ".nf-test",
    "test-data/tiny-index/output/logging",
]

# Target Python version
target-version = "py312"

[tool.mgs-workflow]
# Base image digest (amd64) for container builds — update this to pick up OS-level security patches
container-base-image = "mambaorg/micromamba@sha256:b62ed0c54940e3c801642d72ba7d2462f06356c378eba92d603d39f2ce5e4a0d"
# Minimum pipeline version required to use indexes built with this pipeline version
index-min-pipeline-version = "2.8.1.2"
# Minimum index version required by this pipeline version
pipeline-min-index-version = "3.2.2.0"
# Maximum age (in days) for the benchmark index before CI flags it as stale
max-stable-index-age-days = 90

# Expected output files for each workflow (used by external validation tools)
# Note: {SAMPLE} is a wildcard that matches sample names from the samplesheet
expected-outputs-run = [
    "input/adapters.fasta",
    "input/params-index.json",
    "input/params-run.json",
    "input/samplesheet.csv",
    "logging/pyproject.toml",
    "logging/pyproject-index.toml",
    "results/{SAMPLE}_bracken.tsv.gz",
    "results/{SAMPLE}_kraken.tsv.gz",
    "results/{SAMPLE}_qc_adapter_stats_cleaned.tsv.gz",
    "results/{SAMPLE}_qc_adapter_stats_raw.tsv.gz",
    "results/{SAMPLE}_qc_basic_stats_cleaned.tsv.gz",
    "results/{SAMPLE}_qc_basic_stats_raw.tsv.gz",
    "results/{SAMPLE}_qc_length_stats_cleaned.tsv.gz",
    "results/{SAMPLE}_qc_length_stats_raw.tsv.gz",
    "results/{SAMPLE}_qc_overrepresented_cleaned.tsv.gz",
    "results/{SAMPLE}_qc_overrepresented_raw.tsv.gz",
    "results/{SAMPLE}_qc_quality_base_stats_cleaned.tsv.gz",
    "results/{SAMPLE}_qc_quality_base_stats_raw.tsv.gz",
    "results/{SAMPLE}_qc_quality_sequence_stats_cleaned.tsv.gz",
    "results/{SAMPLE}_qc_quality_sequence_stats_raw.tsv.gz",
    "results/{SAMPLE}_read_counts.tsv",
    "results/{SAMPLE}_virus_hits.tsv.gz",
]

# Platform-specific additions to expected-outputs-run (short-read only)
expected-outputs-run-shortread-extra = [
    "results/{SAMPLE}_fastp.json",
]

expected-outputs-downstream = [
    "input_downstream/input_file.csv",
    "input_downstream/params-downstream.json",
    "logging_downstream/pyproject.toml",
    "results_downstream/{GROUP}_bracken.tsv.gz",
    "results_downstream/{GROUP}_clade_counts.tsv.gz",
    "results_downstream/{GROUP}_duplicate_stats.tsv.gz",
    "results_downstream/{GROUP}_fastp.json",
    "results_downstream/{GROUP}_kraken.tsv.gz",
    "results_downstream/{GROUP}_qc_adapter_stats_cleaned.tsv.gz",
    "results_downstream/{GROUP}_qc_adapter_stats_raw.tsv.gz",
    "results_downstream/{GROUP}_qc_basic_stats_cleaned.tsv.gz",
    "results_downstream/{GROUP}_qc_basic_stats_raw.tsv.gz",
    "results_downstream/{GROUP}_qc_length_stats_cleaned.tsv.gz",
    "results_downstream/{GROUP}_qc_length_stats_raw.tsv.gz",
    "results_downstream/{GROUP}_qc_overrepresented_cleaned.tsv.gz",
    "results_downstream/{GROUP}_qc_overrepresented_raw.tsv.gz",
    "results_downstream/{GROUP}_qc_quality_base_stats_cleaned.tsv.gz",
    "results_downstream/{GROUP}_qc_quality_base_stats_raw.tsv.gz",
    "results_downstream/{GROUP}_qc_quality_sequence_stats_cleaned.tsv.gz",
    "results_downstream/{GROUP}_qc_quality_sequence_stats_raw.tsv.gz",
    "results_downstream/{GROUP}_read_counts.tsv.gz",
    "results_downstream/{GROUP}_validation_hits.tsv.gz",
]

expected-outputs-downstream-ont = [
    "input_downstream/input_file.csv",
    "input_downstream/params-downstream.json",
    "logging_downstream/pyproject.toml",
    "results_downstream/{GROUP}_bracken.tsv.gz",
    "results_downstream/{GROUP}_kraken.tsv.gz",
    "results_downstream/{GROUP}_qc_adapter_stats_cleaned.tsv.gz",
    "results_downstream/{GROUP}_qc_adapter_stats_raw.tsv.gz",
    "results_downstream/{GROUP}_qc_basic_stats_cleaned.tsv.gz",
    "results_downstream/{GROUP}_qc_basic_stats_raw.tsv.gz",
    "results_downstream/{GROUP}_qc_length_stats_cleaned.tsv.gz",
    "results_downstream/{GROUP}_qc_length_stats_raw.tsv.gz",
    "results_downstream/{GROUP}_qc_overrepresented_cleaned.tsv.gz",
    "results_downstream/{GROUP}_qc_overrepresented_raw.tsv.gz",
    "results_downstream/{GROUP}_qc_quality_base_stats_cleaned.tsv.gz",
    "results_downstream/{GROUP}_qc_quality_base_stats_raw.tsv.gz",
    "results_downstream/{GROUP}_qc_quality_sequence_stats_cleaned.tsv.gz",
    "results_downstream/{GROUP}_qc_quality_sequence_stats_raw.tsv.gz",
    "results_downstream/{GROUP}_read_counts.tsv.gz",
    "results_downstream/{GROUP}_validation_hits.tsv.gz",
]
