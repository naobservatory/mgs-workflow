{
  "$schema": "https://datapackage.org/profiles/2.0/tableschema.json",
  "title": "Validated Viral Hits",
  "description": "Per-read viral hit assignments with post-hoc BLAST validation results, as produced by the DOWNSTREAM workflow. Each row represents a single putative viral read. The `aligner_*` columns are inherited from the RUN workflow, where reads are aligned by Bowtie2 (short-read) or Minimap2 (ONT) and then processed into per-read summary statistics; LCA and score statistics are calculated post-hoc across all alignments for each read. Reference sequences are partitioned into three subsets: natural (non-artificial NCBI sequences, unsuffixed columns), artificial (descendants of NCBI taxid 81077, i.e. synthetic constructs and vectors, `_artificial` suffix), and combined (both natural and artificial, `_combined` suffix). The `prim_align_*` columns contain data extracted from the primary alignment record (not secondary or supplementary) in the aligner SAM output. Short-read data is duplicate-marked in two passes: first by alignment coordinates (`prim_align_dup_exemplar`), then by sequence similarity among the reads that survived (`sim_dup_exemplar`, `sim_dup_group_size`); a read that is its own exemplar in both is distinct under both criteria. For validation, reads are grouped by `selected_taxid`, downsampled to at most `params.validation_n_sample` reads per group, and the retained reads are BLASTed against the NCBI core_nt database; the `validation_*` columns therefore describe each read's own alignments and are NA for reads that were not sampled or produced no alignment, as recorded in `validation_status`. Produced for both short-read and ONT platforms; the paired-end-specific columns contain NA values for ONT, where they are appended after the other per-read columns rather than sitting in schema order. Column order is not part of the contract: validation matches the field set, not its arrangement.",
  "fieldsMatch": "equal",
  "missingValues": ["NA"],
  "primaryKey": ["group", "seq_id"],
  "fields": [
    {
      "name": "seq_id",
      "title": "Sequence ID",
      "description": "Unique read identifier (query name from the sequencing instrument).",
      "type": "string",
      "constraints": {
        "required": true,
        "minLength": 1
      },
      "example": "NC_059681.1_0_0"
    },
    {
      "name": "sample",
      "title": "Sample",
      "description": "Sample identifier from the RUN workflow.",
      "type": "string",
      "constraints": {
        "required": true,
        "minLength": 1
      },
      "example": "sample_1"
    },
    {
      "name": "aligner_taxid_lca",
      "title": "Aligner LCA Taxon ID",
      "description": "Lowest common ancestor taxon ID across all alignments for this read, calculated post-hoc from Bowtie2 (short-read) or Minimap2 (ONT) outputs, considering only natural (non-artificial) reference sequences.",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 1
      },
      "example": 2847173
    },
    {
      "name": "aligner_taxid_top",
      "title": "Aligner Top Taxon ID",
      "description": "Taxon ID of the highest-scoring alignment for this read from Bowtie2 (short-read) or Minimap2 (ONT), considering only natural (non-artificial) reference sequences.",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 1
      },
      "example": 2847173
    },
    {
      "name": "aligner_length_normalized_score_mean",
      "title": "Aligner Length-Normalized Score (Mean)",
      "description": "Mean length-normalized alignment score (alignment_score / ln(read_length)) across all alignments for this read, considering only natural (non-artificial) reference sequences.",
      "type": "number",
      "constraints": {
        "required": true,
        "minimum": 0
      },
      "example": 59.39
    },
    {
      "name": "aligner_taxid_lca_combined",
      "title": "Aligner LCA Taxon ID (Combined)",
      "description": "Lowest common ancestor taxon ID across all alignments for this read, calculated post-hoc from aligner outputs, considering all reference sequences (both natural and artificial).",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 1
      },
      "example": 2847173
    },
    {
      "name": "aligner_n_assignments_combined",
      "title": "Aligner Assignment Count (Combined)",
      "description": "Number of reference sequences that the read aligned to, considering all reference sequences (both natural and artificial).",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 1
      },
      "example": 1
    },
    {
      "name": "aligner_length_normalized_score_mean_combined",
      "title": "Aligner Length-Normalized Score (Combined)",
      "description": "Mean length-normalized alignment score (alignment_score / ln(read_length)) across all alignments for this read, considering all reference sequences (both natural and artificial).",
      "type": "number",
      "constraints": {
        "required": true,
        "minimum": 0
      },
      "example": 59.39
    },
    {
      "name": "aligner_taxid_lca_artificial",
      "title": "Aligner LCA Taxon ID (Artificial)",
      "description": "Lowest common ancestor taxon ID across all alignments for this read, calculated post-hoc from aligner outputs, considering only artificial reference sequences (descendants of NCBI taxid 81077). NA when no artificial references were matched.",
      "type": "integer",
      "constraints": {
        "minimum": 1
      },
      "example": 81077
    },
    {
      "name": "aligner_n_assignments_artificial",
      "title": "Aligner Assignment Count (Artificial)",
      "description": "Number of artificial reference sequences (descendants of NCBI taxid 81077) that the read aligned to. 0 when no artificial references were matched.",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 0
      },
      "example": 0
    },
    {
      "name": "aligner_length_normalized_score_mean_artificial",
      "title": "Aligner Length-Normalized Score (Artificial)",
      "description": "Mean length-normalized alignment score (alignment_score / ln(read_length)) across all alignments for this read, considering only artificial reference sequences (descendants of NCBI taxid 81077). NA when no artificial references were matched.",
      "type": "number",
      "constraints": {
        "minimum": 0
      },
      "example": 42.5
    },
    {
      "name": "query_len",
      "title": "Query Length",
      "description": "Read length in bases. For paired-end data, this is mate 1's length.",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 1
      },
      "example": 151
    },
    {
      "name": "query_len_rev",
      "title": "Query Length (Mate 2)",
      "description": "Mate 2's read length in bases. Only present for paired-end (short-read) data; NA for ONT.",
      "type": "integer",
      "constraints": {
        "minimum": 1
      },
      "example": 145
    },
    {
      "name": "query_seq",
      "title": "Query Sequence",
      "description": "Read nucleotide sequence. For paired-end data, this is mate 1's sequence.",
      "type": "string",
      "constraints": {
        "required": true,
        "minLength": 1
      },
      "example": "AGACATCCTGGAAGGGG"
    },
    {
      "name": "query_seq_rev",
      "title": "Query Sequence (Mate 2)",
      "description": "Mate 2's nucleotide sequence. Only present for paired-end (short-read) data; NA for ONT.",
      "type": "string",
      "constraints": {
        "minLength": 1
      },
      "example": "CCCCCTTCTCCAGGACC"
    },
    {
      "name": "query_qual",
      "title": "Query Quality",
      "description": "Base quality scores in Phred+33 ASCII encoding. For paired-end data, these are mate 1's.",
      "type": "string",
      "constraints": {
        "required": true,
        "minLength": 1
      },
      "example": "FFFFFFFFFFFFFFF"
    },
    {
      "name": "query_qual_rev",
      "title": "Query Quality (Mate 2)",
      "description": "Mate 2's base quality scores in Phred+33 ASCII encoding. Only present for paired-end (short-read) data; NA for ONT.",
      "type": "string",
      "constraints": {
        "minLength": 1
      },
      "example": "FFFFFFFFFFFFFFF"
    },
    {
      "name": "prim_align_genome_id_all",
      "title": "Primary Alignment Genome ID",
      "description": "NCBI accession ID(s) of the genome(s) the read aligned to in its primary alignment. For discordant paired-end alignments where the two mates map to different genomes, IDs are joined with '/' in mate 1, mate 2 order.",
      "type": "string",
      "constraints": {
        "required": true,
        "minLength": 1,
        "pattern": "^[^/]+(/[^/]+)?$"
      },
      "example": "NC_059681.1"
    },
    {
      "name": "prim_align_taxid_all",
      "title": "Primary Alignment Taxon ID",
      "description": "NCBI taxon ID(s) corresponding to the genome(s) the read aligned to in its primary alignment. For discordant paired-end alignments where the two mates map to different genomes, IDs are joined with '/' in mate 1, mate 2 order.",
      "type": "string",
      "constraints": {
        "required": true,
        "minLength": 1,
        "pattern": "^[^/]+(/[^/]+)?$"
      },
      "example": "2847173"
    },
    {
      "name": "prim_align_fragment_length",
      "title": "Fragment Length",
      "description": "Inferred fragment length from the primary alignment: Bowtie2's TLEN, the distance between the pair's outer ends counting soft-clipped bases. Only present for paired-end (short-read) data; NA for ONT.",
      "type": "integer",
      "constraints": {
        "minimum": 0
      },
      "example": 917
    },
    {
      "name": "prim_align_best_alignment_score",
      "title": "Best Alignment Score",
      "description": "Best alignment score from the primary alignment. For paired-end data, this is mate 1's score.",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 0
      },
      "example": 298
    },
    {
      "name": "prim_align_best_alignment_score_rev",
      "title": "Best Alignment Score (Mate 2)",
      "description": "Best alignment score for mate 2. Only present for paired-end (short-read) data; NA for ONT.",
      "type": "integer",
      "constraints": {
        "minimum": 0
      },
      "example": 290
    },
    {
      "name": "prim_align_edit_distance",
      "title": "Edit Distance",
      "description": "Edit distance between the read and the aligned reference sequence in the primary alignment (SAM NM tag: mismatches plus inserted and deleted bases). For paired-end data, this is mate 1's edit distance.",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 0
      },
      "example": 0
    },
    {
      "name": "prim_align_edit_distance_rev",
      "title": "Edit Distance (Mate 2)",
      "description": "Edit distance between mate 2 and the aligned reference sequence in the primary alignment (SAM NM tag). Only present for paired-end (short-read) data; NA for ONT.",
      "type": "integer",
      "constraints": {
        "minimum": 0
      },
      "example": 0
    },
    {
      "name": "prim_align_ref_start",
      "title": "Reference Start Position",
      "description": "0-based start position of the alignment on the reference genome, excluding soft-clipped bases. For paired-end data, this is mate 1's position.",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 0
      },
      "example": 149
    },
    {
      "name": "prim_align_ref_start_rev",
      "title": "Reference Start Position (Mate 2)",
      "description": "0-based start position of mate 2's alignment on the reference genome, excluding soft-clipped bases. Only present for paired-end (short-read) data; NA for ONT.",
      "type": "integer",
      "constraints": {
        "minimum": 0
      },
      "example": 921
    },
    {
      "name": "prim_align_ref_start_unclipped",
      "title": "Unclipped Reference Start Position",
      "description": "0-based position of the first reference base the read covers, counting soft- and hard-clipped bases as if they had aligned; `prim_align_ref_start` minus the alignment's leading clip. For paired-end data, this is mate 1's position.",
      "type": "integer",
      "example": 5312
    },
    {
      "name": "prim_align_ref_start_unclipped_rev",
      "title": "Unclipped Reference Start Position (Mate 2)",
      "description": "0-based position of the first reference base mate 2 covers, counting clipped bases as if they had aligned. Only present for paired-end (short-read) data; NA for ONT.",
      "type": "integer",
      "example": 5312
    },
    {
      "name": "prim_align_ref_end_unclipped",
      "title": "Unclipped Reference End Position",
      "description": "0-based position of the last reference base the read covers, counting clipped bases as if they had aligned. For paired-end data, this is mate 1's position.",
      "type": "integer",
      "example": 5461
    },
    {
      "name": "prim_align_ref_end_unclipped_rev",
      "title": "Unclipped Reference End Position (Mate 2)",
      "description": "0-based position of the last reference base mate 2 covers, counting clipped bases as if they had aligned. Only present for paired-end (short-read) data; NA for ONT.",
      "type": "integer",
      "example": 5461
    },
    {
      "name": "prim_align_query_rc",
      "title": "Query Reverse Complement",
      "description": "Whether the read aligned to the reverse strand, and so was reverse-complemented in the primary alignment. For paired-end data, this is mate 1's flag; either mate can align to either strand.",
      "type": "boolean",
      "constraints": {
        "required": true
      },
      "example": false
    },
    {
      "name": "prim_align_query_rc_rev",
      "title": "Query Reverse Complement (Mate 2)",
      "description": "Whether mate 2 aligned to the reverse strand. Only present for paired-end (short-read) data; NA for ONT.",
      "type": "boolean",
      "example": true
    },
    {
      "name": "prim_align_pair_status",
      "title": "Pair Status",
      "description": "Bowtie2 pair alignment status code. CP = concordant pair, DP = discordant pair, UP = unpaired. Only present for paired-end (short-read) data; NA for ONT.",
      "type": "string",
      "constraints": {
        "enum": ["CP", "DP", "UP"]
      },
      "example": "DP"
    },
    {
      "name": "group",
      "title": "Group",
      "description": "Group identifier assigned to this set of samples.",
      "type": "string",
      "constraints": {
        "required": true,
        "minLength": 1
      },
      "example": "group_1"
    },
    {
      "name": "prim_align_dup_exemplar",
      "title": "Duplicate Exemplar Read ID",
      "description": "Read ID of the exemplar selected to represent this read's alignment-based duplicate group (based on alignment coordinates, not sequence similarity). A read that is its own exemplar (seq_id == prim_align_dup_exemplar) is either unique or the chosen representative of its duplicate group. Only present for short-read data; NA for ONT.",
      "type": "string",
      "constraints": {
        "minLength": 1
      },
      "example": "NC_059681.1_0_0"
    },
    {
      "name": "sim_dup_exemplar",
      "title": "Similarity Duplicate Exemplar Read ID",
      "description": "Read ID of the exemplar selected to represent this read's similarity-based duplicate group. Similarity marking runs after alignment-based marking and considers only the reads that survived it, clustering their sequences by minimizers (via nao-dedup). NA for reads that are already alignment duplicates (seq_id != prim_align_dup_exemplar), which similarity marking never examines. A read that is its own exemplar (seq_id == sim_dup_exemplar) survived both passes. Only present for short-read data; NA for ONT.",
      "type": "string",
      "constraints": {
        "minLength": 1
      },
      "example": "NC_059681.1_0_0"
    },
    {
      "name": "sim_dup_group_size",
      "title": "Similarity Duplicate Group Size",
      "description": "Number of reads this similarity exemplar represents, before either deduplication pass: the members of its similarity group plus each of those members' alignment duplicates. Populated only on the surviving exemplar; NA on every other read, including alignment duplicates and reads that are themselves similarity duplicates. Only present for short-read data; NA for ONT.",
      "type": "integer",
      "constraints": {
        "minimum": 1
      },
      "example": 3
    },
    {
      "name": "selected_taxid",
      "title": "Selected Taxon ID",
      "description": "Taxon ID used to group reads for validation: the species-level ancestor of the aligner LCA assignment where one exists, otherwise the aligner LCA taxon ID itself. Reads are downsampled independently within each selected taxid, so this is the stratum the read's validation sample was drawn from.",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 1
      },
      "example": 12475
    },
    {
      "name": "validation_staxid_lca_combined",
      "title": "Validation LCA Taxon ID (Combined)",
      "description": "LCA taxon ID from BLAST validation of the read against core_nt, considering all reference sequences (both natural and artificial). NA when the read was not selected for validation, or when no hits were retained after filtering.",
      "type": "integer",
      "constraints": {
        "minimum": 1
      },
      "example": 12475
    },
    {
      "name": "validation_n_assignments_combined",
      "title": "Validation Assignment Count (Combined)",
      "description": "Number of BLAST assignments retained after filtering, considering all reference sequences (both natural and artificial). NA when the read was not selected for validation or produced no alignment.",
      "type": "integer",
      "constraints": {
        "minimum": 0
      },
      "example": 1
    },
    {
      "name": "validation_n_assignments_classified_combined",
      "title": "Validation Classified Assignment Count (Combined)",
      "description": "Number of classified BLAST assignments (with known taxonomic classification), considering all reference sequences (both natural and artificial). NA when the read was not selected for validation or produced no alignment; 0 when the read aligned but no classified hits were retained.",
      "type": "integer",
      "constraints": {
        "minimum": 0
      },
      "example": 1
    },
    {
      "name": "validation_staxid_top_combined",
      "title": "Validation Top Taxon ID (Combined)",
      "description": "Taxon ID of the top-scoring BLAST hit, considering all reference sequences (both natural and artificial). NA when the read was not selected for validation, or when no hits were retained after filtering.",
      "type": "integer",
      "constraints": {
        "minimum": 1
      },
      "example": 12475
    },
    {
      "name": "validation_staxid_top_classified_combined",
      "title": "Validation Top Hit Classified (Combined)",
      "description": "Whether the top-scoring BLAST hit has a known taxonomic classification, considering all reference sequences (both natural and artificial). NA when the read was not selected for validation, or when no hits were retained after filtering.",
      "type": "boolean",
      "example": true
    },
    {
      "name": "validation_bitscore_min_combined",
      "title": "Validation Min Bitscore (Combined)",
      "description": "Minimum bitscore among retained BLAST hits, considering all reference sequences (both natural and artificial). NA when the read was not selected for validation, or when no hits were retained after filtering.",
      "type": "number",
      "constraints": {
        "minimum": 0
      },
      "example": 276.0
    },
    {
      "name": "validation_bitscore_max_combined",
      "title": "Validation Max Bitscore (Combined)",
      "description": "Maximum bitscore among retained BLAST hits, considering all reference sequences (both natural and artificial). NA when the read was not selected for validation, or when no hits were retained after filtering.",
      "type": "number",
      "constraints": {
        "minimum": 0
      },
      "example": 276.0
    },
    {
      "name": "validation_bitscore_mean_combined",
      "title": "Validation Mean Bitscore (Combined)",
      "description": "Mean bitscore among retained BLAST hits, considering all reference sequences (both natural and artificial). NA when the read was not selected for validation, or when no hits were retained after filtering.",
      "type": "number",
      "constraints": {
        "minimum": 0
      },
      "example": 276.0
    },
    {
      "name": "validation_staxid_lca",
      "title": "Validation LCA Taxon ID",
      "description": "LCA taxon ID from BLAST validation, considering only natural (non-artificial) reference sequences. NA when the read was not selected for validation, or when no natural hits were retained after filtering.",
      "type": "integer",
      "constraints": {
        "minimum": 1
      },
      "example": 12475
    },
    {
      "name": "validation_n_assignments",
      "title": "Validation Assignment Count",
      "description": "Number of BLAST assignments retained after filtering, considering only natural (non-artificial) reference sequences. NA when the read was not selected for validation or produced no alignment; 0 when the read aligned but no natural hits were retained.",
      "type": "integer",
      "constraints": {
        "minimum": 0
      },
      "example": 1
    },
    {
      "name": "validation_n_assignments_classified",
      "title": "Validation Classified Assignment Count",
      "description": "Number of classified BLAST assignments (with known taxonomic classification), considering only natural (non-artificial) reference sequences. NA when the read was not selected for validation or produced no alignment; 0 when the read aligned but no classified natural hits were retained.",
      "type": "integer",
      "constraints": {
        "minimum": 0
      },
      "example": 1
    },
    {
      "name": "validation_staxid_top",
      "title": "Validation Top Taxon ID",
      "description": "Taxon ID of the top-scoring BLAST hit, considering only natural (non-artificial) reference sequences. NA when the read was not selected for validation, or when no natural hits were retained after filtering.",
      "type": "integer",
      "constraints": {
        "minimum": 1
      },
      "example": 12475
    },
    {
      "name": "validation_staxid_top_classified",
      "title": "Validation Top Hit Classified",
      "description": "Whether the top-scoring BLAST hit has a known taxonomic classification, considering only natural (non-artificial) reference sequences. NA when the read was not selected for validation, or when no natural hits were retained after filtering.",
      "type": "boolean",
      "example": true
    },
    {
      "name": "validation_bitscore_min",
      "title": "Validation Min Bitscore",
      "description": "Minimum bitscore among retained BLAST hits, considering only natural (non-artificial) reference sequences. NA when the read was not selected for validation, or when no natural hits were retained after filtering.",
      "type": "number",
      "constraints": {
        "minimum": 0
      },
      "example": 276.0
    },
    {
      "name": "validation_bitscore_max",
      "title": "Validation Max Bitscore",
      "description": "Maximum bitscore among retained BLAST hits, considering only natural (non-artificial) reference sequences. NA when the read was not selected for validation, or when no natural hits were retained after filtering.",
      "type": "number",
      "constraints": {
        "minimum": 0
      },
      "example": 276.0
    },
    {
      "name": "validation_bitscore_mean",
      "title": "Validation Mean Bitscore",
      "description": "Mean bitscore among retained BLAST hits, considering only natural (non-artificial) reference sequences. NA when the read was not selected for validation, or when no natural hits were retained after filtering.",
      "type": "number",
      "constraints": {
        "minimum": 0
      },
      "example": 276.0
    },
    {
      "name": "validation_staxid_lca_artificial",
      "title": "Validation LCA Taxon ID (Artificial)",
      "description": "LCA taxon ID from BLAST validation, considering only artificial reference sequences (descendants of NCBI taxid 81077). NA when the read was not selected for validation, or when no artificial hits were retained after filtering.",
      "type": "integer",
      "constraints": {
        "minimum": 1
      },
      "example": 81077
    },
    {
      "name": "validation_n_assignments_artificial",
      "title": "Validation Assignment Count (Artificial)",
      "description": "Number of BLAST assignments retained after filtering, considering only artificial reference sequences (descendants of NCBI taxid 81077). NA when the read was not selected for validation or produced no alignment; 0 when the read aligned but no artificial hits were retained.",
      "type": "integer",
      "constraints": {
        "minimum": 0
      },
      "example": 0
    },
    {
      "name": "validation_n_assignments_classified_artificial",
      "title": "Validation Classified Assignment Count (Artificial)",
      "description": "Number of classified BLAST assignments (with known taxonomic classification), considering only artificial reference sequences (descendants of NCBI taxid 81077). NA when the read was not selected for validation or produced no alignment; 0 when the read aligned but no classified artificial hits were retained.",
      "type": "integer",
      "constraints": {
        "minimum": 0
      },
      "example": 0
    },
    {
      "name": "validation_staxid_top_artificial",
      "title": "Validation Top Taxon ID (Artificial)",
      "description": "Taxon ID of the top-scoring BLAST hit, considering only artificial reference sequences (descendants of NCBI taxid 81077). NA when the read was not selected for validation, or when no artificial hits were retained after filtering.",
      "type": "integer",
      "constraints": {
        "minimum": 1
      },
      "example": 81077
    },
    {
      "name": "validation_staxid_top_classified_artificial",
      "title": "Validation Top Hit Classified (Artificial)",
      "description": "Whether the top-scoring BLAST hit has a known taxonomic classification, considering only artificial reference sequences (descendants of NCBI taxid 81077). NA when the read was not selected for validation, or when no artificial hits were retained after filtering.",
      "type": "boolean",
      "example": true
    },
    {
      "name": "validation_bitscore_min_artificial",
      "title": "Validation Min Bitscore (Artificial)",
      "description": "Minimum bitscore among retained BLAST hits, considering only artificial reference sequences (descendants of NCBI taxid 81077). NA when the read was not selected for validation, or when no artificial hits were retained after filtering.",
      "type": "number",
      "constraints": {
        "minimum": 0
      },
      "example": 150.0
    },
    {
      "name": "validation_bitscore_max_artificial",
      "title": "Validation Max Bitscore (Artificial)",
      "description": "Maximum bitscore among retained BLAST hits, considering only artificial reference sequences (descendants of NCBI taxid 81077). NA when the read was not selected for validation, or when no artificial hits were retained after filtering.",
      "type": "number",
      "constraints": {
        "minimum": 0
      },
      "example": 200.0
    },
    {
      "name": "validation_bitscore_mean_artificial",
      "title": "Validation Mean Bitscore (Artificial)",
      "description": "Mean bitscore among retained BLAST hits, considering only artificial reference sequences (descendants of NCBI taxid 81077). NA when the read was not selected for validation, or when no artificial hits were retained after filtering.",
      "type": "number",
      "constraints": {
        "minimum": 0
      },
      "example": 175.0
    },
    {
      "name": "validation_distance_aligner",
      "title": "Validation Distance (Aligner)",
      "description": "Taxonomic distance (number of parent-child steps) from the original aligner LCA taxon ID to the LCA of both the aligner and validation assignments. NA unless `validation_status` is `aligned` and natural hits were retained after filtering.",
      "type": "integer",
      "constraints": {
        "minimum": 0
      },
      "example": 1
    },
    {
      "name": "validation_distance_validation",
      "title": "Validation Distance (Validation)",
      "description": "Taxonomic distance (number of parent-child steps) from the validation LCA taxon ID to the LCA of both the aligner and validation assignments. NA unless `validation_status` is `aligned` and natural hits were retained after filtering.",
      "type": "integer",
      "constraints": {
        "minimum": 0
      },
      "example": 0
    },
    {
      "name": "validation_status",
      "title": "Validation Status",
      "description": "Whether this read was validated on its own evidence. `aligned`: the read was selected for validation and BLAST returned at least one hit passing the score filters, so the `validation_*` columns describe this read's own alignments. `no_alignment`: the read was selected for validation but no hit survived filtering, so the `validation_*` columns are NA. `not_sampled`: the read was not selected for validation, so the `validation_*` columns are NA. Validation results are never propagated between reads.",
      "type": "string",
      "constraints": {
        "required": true,
        "enum": ["aligned", "no_alignment", "not_sampled"]
      },
      "example": "aligned"
    }
  ]
}
